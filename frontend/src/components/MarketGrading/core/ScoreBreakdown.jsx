/**
 * ScoreBreakdown.jsx
 *
 * Displays the breakdown of how a market grade is calculated,
 * showing individual metric contributions and weights.
 *
 * Props:
 * - metrics: array of { name, score, weight, contribution }
 * - totalScore: number
 * - showWeights: boolean
 *
 * Usage:
 * <ScoreBreakdown metrics={metricsArray} totalScore={85} />
 */

import React from 'react';

const ScoreBreakdown = ({ metrics, totalScore, showWeights = true }) => {
  // TODO: Implement score breakdown component
  return null;
};

export default ScoreBreakdown;
