/**
 * MapLegend.jsx
 *
 * Reusable legend component for grade-colored maps.
 * Shows grade scale with colors and optional score ranges.
 *
 * Props:
 * - type: 'grade' | 'score' | 'hhi'
 * - orientation: 'horizontal' | 'vertical'
 * - showLabels: boolean
 * - compact: boolean
 *
 * Usage:
 * <MapLegend type="grade" orientation="horizontal" />
 */

import React from 'react';

const MapLegend = ({
  type = 'grade',
  orientation = 'horizontal',
  showLabels = true,
  compact = false
}) => {
  // TODO: Implement map legend component
  return null;
};

export default MapLegend;
